<div class='page'>
  <h1><%= @header %></h1>

  <% @jobs.each do |job| %>
    <p><%= link_to "#{job.title} at #{job.company.name}", job_path(job) %></p>
  <% end %><br/>

  <h4><%= link_to 'Create a New Job', new_job_path %></h4><br/><br/>
</div>
