
<div class=show_job>
  <h1><%= @job.title %> at <%= link_to @company.name, company_path(@company) %></h1>
  <h4><%= link_to "Edit", edit_job_path(@job) %></h4>
  <h4><%= link_to "Delete", job_path(@job), method: "delete" %></h4>


  <p>City: <%= @job.city %></p>

  <p>Level of Interest: <%= @job.level_of_interest %></p>

  <% if @job.description != ""%>
  <p>Description: <%= @job.description %></p>
  <% end %>
  <h3><%= link_to "See other #{@category.title} jobs", category_path(@category) %></h3>

  <br/><br/>
  Comments: <br/><br/>
  <%= form_for [@job, @comment] do |f| %>
    <%= f.label :content %>
    <%= f.text_field :content %>

    <%= f.submit %>
  <% end %><br/>

  <% @comments.each do |comment| %>
    <%= comment.content %>
    <%= comment.created_at %><br/><br/>
  <% end %>
</div>
