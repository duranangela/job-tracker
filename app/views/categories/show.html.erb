<h1><%= @category.title %></h1>

<% @jobs.each do |job| %>
  <%= link_to "#{job.title} at #{job.company.name}", company_job_path(job.company, job) %>
<% end %>
<br/><br/>
<%= link_to '<< Back to Category List', categories_path %>
